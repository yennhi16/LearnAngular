import { TrigonometryDirective } from './trigonometry.directive';

describe('TrigonometryDirective', () => {
  it('should create an instance', () => {
    const directive = new TrigonometryDirective();
    expect(directive).toBeTruthy();
  });
});
