import { IfLoadedDirective } from './if-loaded.directive';

describe('IfLoadedDirective', () => {
  it('should create an instance', () => {
    const directive = new IfLoadedDirective();
    expect(directive).toBeTruthy();
  });
});
